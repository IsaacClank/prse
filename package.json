{
  "name": "prse",
  "version": "0.1.1",
  "description": "Minimal cli parser library",
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "files": [
    "dist/src/"
  ],
  "license": "GPL-3.0-or-later",
  "author": "IsaacClank",
  "repository": "github.com/IsaacClank/prse.git",
  "scripts": {
    "tsc": "pnpx tsc",
    "dev": "pnpm tsc && node dist/index.js",
    "test": "pnpm tsc && node dist/test.js",
    "clean": "rm -rf node_modules dist build"
  },
  "devDependencies": {
    "@types/node": "^14.14.34",
    "prve": "^0.1.3",
    "typescript": "^4.2.3"
  }
}
